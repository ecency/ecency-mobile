import EStyleSheet from 'react-native-extended-stylesheet';

export default EStyleSheet.create({
  communityCard: {
    flexDirection: 'row',
    height: 60,
    minWidth: '100%',
    alignItems: 'center',
  },
  text: {
    marginHorizontal: 8,
  },
});
