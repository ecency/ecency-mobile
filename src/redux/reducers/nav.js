// import Navigator from '../../navigation/appNavigator';
//TOOD: put back state for action
export default (state, action) => {
  // const newState = Navigator.router.getStateForAction(action, state);
  return {}; //newState || state;
};
